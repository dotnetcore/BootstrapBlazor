@page "/modbus-factory"
@inject IStringLocalizer<ModbusFactories> Localizer

<h3>@((MarkupString)Localizer["Title"].Value)</h3>
<h4>@Localizer["Description"]</h4>

<PackageTips Name="Longbow.Modbus" />

<Tips><div>@((MarkupString)Localizer["WasmTips"].Value)</div></Tips>

<p class="code-label">@Localizer["SectionServiceInjection"]</p>

<Pre>services.AddModbusFactory();</Pre>

<Pre>[Inject]
[NotNull]
private IModbusFactory? ModbusFactory { get; set; }</Pre>

<p class="code-label">@Localizer["SectionServiceUsage"]</p>
<p>@((MarkupString)Localizer["ServiceUsageIntro"].Value)</p>

<p class="code-label">@((MarkupString)Localizer["SectionFactoryClients"].Value)</p>

<p>@((MarkupString)Localizer["ClientDerivedIntro"].Value)</p>

<ul class="ul-demo">
    <li>@((MarkupString)Localizer["ClientTypeSocket"].Value)</li>
    <li>@((MarkupString)Localizer["ClientTypeSerialPort"].Value)</li>
</ul>

<p>@Localizer["TransportMediaIntro"]</p>

<ul class="ul-demo">
    <li>@((MarkupString)Localizer["TransportRtu"].Value)</li>
    <li>@((MarkupString)Localizer["TransportTcp"].Value)</li>
</ul>

<p>@((MarkupString)Localizer["FactoryMethodsTitle"].Value)</p>

<ul class="ul-demo">
    <li>@((MarkupString)Localizer["FactoryMethodTcp"].Value)</li>
    <li>@((MarkupString)Localizer["FactoryMethodUdp"].Value)</li>
    <li>@((MarkupString)Localizer["FactoryMethodRtu"].Value)</li>
    <li>@((MarkupString)Localizer["FactoryMethodRtuOverTcp"].Value)</li>
    <li>@((MarkupString)Localizer["FactoryMethodRtuOverUdp"].Value)</li>
</ul>

<p>@((MarkupString)Localizer["RemoveIntro"].Value)</p>

<Pre>ModbusFactory.RemoveTcpMaster("test");</Pre>

<p class="code-label">@Localizer["SectionDataOperation"]</p>

<p>@((MarkupString)Localizer["DataTypeIntro"].Value)</p>

<ul class="ul-demo">
    <li>@Localizer["DataTypeCoils"]</li>
    <li>@Localizer["DataTypeDiscreteInputs"]</li>
    <li>@Localizer["DataTypeInputRegisters"]</li>
    <li>@Localizer["DataTypeHoldingRegisters"]</li>
</ul>

<p>@((MarkupString)Localizer["ClientMethodsIntro"].Value)</p>

<ul class="ul-demo">
    <li>@((MarkupString)Localizer["ClientMethodCoils"].Value)</li>
    <li>@((MarkupString)Localizer["ClientMethodDiscreteInputs"].Value)</li>
    <li>@((MarkupString)Localizer["ClientMethodInputRegisters"].Value)</li>
    <li>@((MarkupString)Localizer["ClientMethodHoldingRegisters"].Value)</li>
</ul>

<p>@Localizer["ProtocolLimitIntro"]</p>

<ul class="ul-demo">
    <li>@((MarkupString)Localizer["ProtocolLimitCoils"].Value)</li>
    <li>@((MarkupString)Localizer["ProtocolLimitDiscreteInputs"].Value)</li>
    <li>@((MarkupString)Localizer["ProtocolLimitInputRegisters"].Value)</li>
    <li>@((MarkupString)Localizer["ProtocolLimitHoldingRegisters"].Value)</li>
</ul>

<p>@((MarkupString)Localizer["ResponseDefinitionIntro"].Value)</p>

<Pre>@((MarkupString)Localizer["ResponseInterfaceCode"].Value)</Pre>

<p>@((MarkupString)Localizer["ResponseExtensionsIntro"].Value)</p>

<ul class="ul-demo">
    <li>@((MarkupString)Localizer["ResponseExtensionBool"].Value)</li>
    <li>@((MarkupString)Localizer["ResponseExtensionUShort"].Value)</li>
    <li>@((MarkupString)Localizer["ResponseExtensionFloatABCD"].Value)</li>
    <li>@((MarkupString)Localizer["ResponseExtensionFloatCDAB"].Value)</li>
    <li>@((MarkupString)Localizer["ResponseExtensionFloatBADC"].Value)</li>
    <li>@((MarkupString)Localizer["ResponseExtensionFloatDCBA"].Value)</li>
</ul>

<p>@((MarkupString)Localizer["FloatCustomExtensionIntro"].Value)</p>

<p>@((MarkupString)Localizer["FloatEndiannessTips"].Value)</p>

<p>@((MarkupString)Localizer["FloatUsageIntro"].Value)</p>

<Pre>var response = await client.ReadHoldingRegistersAsync(0x01, 0x00, 2);

var value1 = response.ReadFloatABCDValue(0);
var value2 = response.ReadFloatCDABValue(0);
var value3 = response.ReadFloatBADCValue(0);
var value4 = response.ReadFloatDCBAValue(0);</Pre>

<p>@((MarkupString)Localizer["FloatIndexIntro"].Value)</p>

<p>@Localizer["BenchmarkProjectIntro"]</p>

<Pre>private const int NumberOfTask = 10;
private const int TaskNumberOfClient = 10;
private const int ClientCount = 10;

private async Task InitLongbowModbus()
{
    var sc = new ServiceCollection();
    sc.AddModbusFactory();

    var provider = sc.BuildServiceProvider();
    var factory = provider.GetRequiredService&lt;IModbusFactory&gt;();

    for (var index = 0; index &lt; ClientCount; index++)
    {
        var client = factory.GetOrCreateTcpMaster();
        await client.ConnectAsync("127.0.0.1", 502);
        await client.ReadHoldingRegistersAsync(0x01, 0x00, 100);

        _lgbModbusClients.Add(client);
    }
}

[Benchmark]
public async Task LongbowModbus()
{
    var tasks = _lgbModbusClients.SelectMany(c =>
    {
        var tasks = new List&lt;Task&gt;();
        for (int i = 0; i &lt; TaskNumberOfClient; i++)
        {
            tasks.Add(Task.Run(async () =&gt;
            {
                for (int i = 0; i &lt; NumberOfTask; i++)
                {
                    var d = await c.ReadHoldingRegistersAsync(1, 0, 100);
                }
            }));
        }
        return tasks;
    }).ToList();

    await Task.WhenAll(tasks);
}</Pre>

<p>@((MarkupString)Localizer["BenchmarkSummary"].Value)</p>

<p>@Localizer["BenchmarkResultIntro"]</p>

    <Pre>| Method            | Mean    | Error    | StdDev   | Allocated |
|------------------ |--------:|---------:|---------:|----------:|
| LongbowModbus     | 2.442 s | 0.0471 s | 0.0503 s |   1.14 MB |
| NModbus           | 4.752 s | 0.1544 s | 0.4552 s |    3.3 MB |</Pre>
