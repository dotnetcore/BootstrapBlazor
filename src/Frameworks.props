<Project>

  <PropertyGroup>
    <TargetFrameworks Condition="'$(Configuration)'=='Debug'">net6.0</TargetFrameworks>
    <TargetFrameworks Condition="'$(Configuration)'!='Debug'">net5.0;net6.0</TargetFrameworks>
  </PropertyGroup>

</Project>
