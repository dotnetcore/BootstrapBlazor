@namespace BootstrapBlazor.Components
@inherits ButtonBase

<button @attributes="AdditionalAttributes" class="@ButtonClassName" id="@Id" disabled="@Disabled" role="button" aria-disabled="@DisabledString" OnClick="OnClickButton">
    @if (!string.IsNullOrEmpty(Icon))
    {
        <i class="@Icon"></i>
    }
    @if (!string.IsNullOrEmpty(ImageUrl))
    {
        <img alt="img" src="@ImageUrl" />
    }
    @if (!string.IsNullOrEmpty(Text))
    {
        <span>@Text</span>
    }
    @ChildContent
    <div class="@PulseColorString">
    </div>
</button>
