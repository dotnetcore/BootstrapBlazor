@namespace BootstrapBlazor.Components
@inherits BootstrapComponentBase

<Modal AdditionalAttributes="@AdditionalAttributes" @ref="ModalContainer" IsKeyboard="@IsKeyboard">
    @foreach (var parameter in DialogParameters)
    {
        @Dialog.RenderDialog(parameter)
    }
</Modal>
