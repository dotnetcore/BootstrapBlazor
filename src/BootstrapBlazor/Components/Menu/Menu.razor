@namespace BootstrapBlazor.Components
@inherits MenuBase

<nav @attributes="@AdditionalAttributes" class="@ClassString" @ref="@MenuElement">
    @if (IsVertical)
    {
        <SideMenu Items="@Items" DisableNavigation="@DisableNavigation" IsAccordion="@IsAccordion" OnClick="@OnClick" />
    }
    else
    {
        <TopMenu Items="@Items" DisableNavigation="@DisableNavigation" OnClick="@OnClick" />
    }
</nav>
