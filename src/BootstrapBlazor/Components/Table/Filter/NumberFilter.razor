@namespace BootstrapBlazor.Components
@inherits ValueTypeConditionBase

<Select TItem="FilterAction" Items="@Items" @bind-Value="@Action1"></Select>

<BootstrapInput TItem="int?" @bind-Value="@Value1" ShowLabel="false"></BootstrapInput>

@if (Count > 0)
{
    <FilterLogicItem @bind-Logic="Logic"></FilterLogicItem>

    <Select TItem="FilterAction" Items="@Items" @bind-Value="@Action2"></Select>

    <BootstrapInput TItem="int?" @bind-Value="@Value2" ShowLabel="false"></BootstrapInput>
}