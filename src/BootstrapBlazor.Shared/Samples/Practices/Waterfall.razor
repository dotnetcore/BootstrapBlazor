@page "/practice/waterfall"

<div class="container-fluid">
    <div class="row">
        <div class="card-columns">
            @foreach (var item in IamgeList)
            {
                <ImageViewer class="card-img" Url="@item" PreviewList="@(new List<string>(){item})" ShowPlaceHolder="true" />
            }
        </div>
    </div>
</div>
