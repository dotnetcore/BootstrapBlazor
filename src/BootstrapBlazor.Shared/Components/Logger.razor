<div class="@ClassName">
    @foreach (var m in Message.Reverse())
    {
        <div class="log-item">
            @if (IsHtml)
            {
                @((MarkupString)m)
            }
            else
            {
                @m
            }
        </div>
    }
</div>
