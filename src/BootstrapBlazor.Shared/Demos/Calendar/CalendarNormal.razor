<Calendar ValueChanged="@OnValueChanged" />
<ConsoleLogger @ref="Logger" class="mt-3" />

@code {
    [NotNull]
    private ConsoleLogger? Logger { get; set; }

    private void OnValueChanged(DateTime ts)
    {
        Logger.Log($"{ts:yyyy-MM-dd}");
    }
}
