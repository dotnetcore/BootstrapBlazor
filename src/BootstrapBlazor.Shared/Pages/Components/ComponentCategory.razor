<div class="coms-cate">
    <h3>
        <span>@Text</span>
    </h3>

    <h4>@Desc</h4>

    <div class="coms-demo">
        <div class="row g-3">
            <CascadingValue Value="this" IsFixed="true">
                @ChildContent
            </CascadingValue>
        </div>
    </div>

    <RenderTemplate>
        @if (Cards.Count > 0)
        {
            <Badge IsPill="true" Color="Color.Success">@Cards.Count</Badge>
        }
    </RenderTemplate>
</div>
