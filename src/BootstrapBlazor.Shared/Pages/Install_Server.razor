@page "/install-server"
@inject IStringLocalizer<Install_Server> Localizer

<InstallContent Title="@Localizer["Title"]">
<Pre>public void ConfigureServices(IServiceCollection services)
{
    services.AddRazorPages();
    services.AddServerSideBlazor();

    // @Localizer["CodeComment"]
    <b><i>services.AddBootstrapBlazor();</i></b>
}</Pre>

<h4>@Localizer["Configure"]</h4>
<Pre>public void Configure(IApplicationBuilder app)
{
    // @Localizer["CodeComment"]
    <b><i>app.ApplicationServices.RegisterProvider();</i></b>
}</Pre>

</InstallContent>
