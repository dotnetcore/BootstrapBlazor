# BootstrapBlazor Claude Rules

Follow `AGENTS.md` for BootstrapBlazor component code generation.

Before generating component code, read `skill-index.json`, then read the current component source. Component Skill files are stored under `docs/skills/components` and are linked from the index. If an official Sample exists, analyze it before relying on Skill examples.
